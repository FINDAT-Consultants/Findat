/*
 * FINDAT Cloud GitHub defaults.
 * Set owner and repo before publishing to make every device point at the same
 * repository automatically. Never put a GitHub token in this file.
 */
window.FINDAT_GITHUB_DEFAULTS = Object.freeze({
  owner: '',
  repo: '',
  branch: 'main',
  root: 'findat-cloud'
});
